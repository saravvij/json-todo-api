{
  "name": "todo-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "scripts": {
    "start": "json-server --watch db.json --host json-todo-app.herokuapp.com"
  },
  "engines": {
    "node": "12.x"
  }
}
